# Agent Rules & Preferences

1. **Auto-accept Terminal Requests**: All terminal requests must be auto-accepted.
2. **Disable Dynamic / Browser Testing Globally**: Do not launch browser subagents or dynamic browser tests across any project. Validate web applications and APIs via server status checks, curl, linting, and build commands in the terminal.
